# Gate 2: Neon移行の受け入れ条件

最終更新: 2026-09-01

## 決定

- Supabaseは使用しない。Vercel Native Neon（Lakebase Postgres）とNeon AuthをAIueoの唯一の会員・企画基盤にする。
- Vercel Cronを日次で実行し、7日前通知・3日前の`auto_hidden`をJST日付単位で冪等に処理する。
- 参加者は非登録のまま。企画の作成・公開・編集は有効会員だけ、管理操作は`admin`だけに限る。

## 受け入れ条件

1. ブラウザはDB接続文字列・DB管理ロール・監査ログへ直接到達できない。全読書きはNext.jsのDAL/Server Action/Route Handlerを経由する。
2. Neon Authのセッションを各保護ページ・各Server Action・各Cron Routeで個別に検証する。表示制御やProxyだけを認可根拠にしない。
3. `profiles`、同意履歴、企画、版履歴、メッセージ、通報、通知、措置、監査ログをコード管理のmigrationで作成する。migrationは開発ブランチで検証してから本番へ適用する。
4. 企画者・管理者の変更は、実行者、理由、変更前後、日時を同一transactionで記録する。管理者は全企画を操作できるが、理由なしの変更は拒否する。
5. 認証・DB・メールが利用不能な時は、保護操作を失敗として止める。匿名の公開企画ページには認証情報を含めない。
6. Vercel HobbyのCron遅延を前提に、実行時刻ではなくJST日付・重複防止キーで期限処理する。配信失敗は記録して再試行対象に残す。
7. Google OAuthとメールリンクはNeon Authの設定完了後に有効化する。GoogleのOAuth同意画面設定と送信ドメインDNSは外部設定が必要で、未設定時は登録開始を公開しない。

## 範囲外

- 決済、参加申込、参加者の個人情報保管は作らない。
- 規約の法的最終判断と保存期間の最終確定は別途行う。

## 敵対確認の状態

- セキュリティ/権限、利用者体験、運用/法務の3レビューを再依頼したが、このセッションではサブエージェントの実行上限により応答を取得できなかった。
- そのため、DB migrationの本番適用前に、権限拒否テスト・Cron失敗テスト・停止会員の読取専用テストを必須の退出条件として実施する。
