# 失敗記録

このプロジェクトで実際に起きた失敗を、1か所に集めた正本である。作業を始める前に読む。
`.claude/hooks/session-start.sh` がセッション開始時にこのファイルの件数と見出しを画面へ出す。

書く目的は反省ではない。**同じ失敗を繰り返さないための、検知手段の一覧**である。
だから各件に「再発を止める手段」を必ず書く。手段が無い件は「手段なし」と書いて残す。

新しい失敗が起きたら、消さずに追記する。件数が増えることは悪いことではない。
繰り返すことが悪い。

---

## F-01 「実装した」を「完了」と書き、通しで動かさずに終わった

**いつ**: 2026-08-31 から 2026-09-10 まで継続。単発ではなく、この台帳の全期間にわたる。

**何が起きたか**: `IMPLEMENTATION_PLAN.md` のフェーズ表で、次のフェーズがすべて
「完了」と書かれている。しかし括弧の中は「実DBでの動作確認は未実施」
「実データでの表示確認は未実施」「実機確認はユーザー待ち」である。

P3・P4・P5（状態遷移の自動E2E未実施）、P15（実Googleアカウントでの通し確認が未実施）、
P17（実企画データでの見た目確認が未実施）、P19（所有者チェック・状態遷移の実DB確認が未実施）、
P20（規約更新後の実DB確認が未実施）、P21（実データでの表示確認が未実施）、
P22（管理者が理由を入力した状態での表示確認が未実施）、P26（ログイン状態での表示確認が未実施）、
P27（「本番反映済み」で終えた直後にGoogle 500で登録へ到達できず）。

**何が悪かったか**: 「完了」の定義が「コードを書いてデプロイした」になっていた。
利用者が使えるかどうかは一度も確かめていない。そのうえで正本に「完了」と書いたので、
次のセッションはそれを前提に次のフェーズへ進んだ。**動いていない機能の上に、
動いていない機能が積み上がった。**

**再発を止める手段**:
- `AGENTS.md` §7「完了と書ける条件」。通しで動かしていない区間があるフェーズは
  「完了」と書かない。状態欄の**先頭**に「未検証区間: 〜」を書く。
- 目視確認の依頼で終わらせない。確かめる手段を機械で残す（下の F-02 参照）。

---

## F-02 確認をユーザーへ丸投げし、誤った記述が正本に残った

**いつ**: 2026-09-09（発覚は 2026-09-10）。

**何が起きたか**: P15（Google認証への切替）で、引継ぎに次のように書いて終わった。

> Neon ConsoleのGoogleプロバイダーを共用鍵から自前の鍵へ切替済み（バッジ `Shared keys` が消えたことを確認）
> ／ Googleの画面に **`AIueo`** と表示されることを確認（自前の鍵が効いている証拠）

翌日、実際に本番を操作すると、Googleの画面は **`to continue to neon.tech`** と表示された。
`AIueo` ではない。さらにユーザーのGoogleアカウントに届いた通知メール（2026-09-10 07:21 JST、
`noreply-accounts@google.com`）の件名は
**「Google アカウントの一部のデータを neon.tech と共有しました」** だった。
Google自身の記録が、使われたOAuthアプリを `neon.tech` だと言っている。

**何が悪かったか**: 「確認してほしい」という**目で見る依頼**にしたため、
誰も確認しないまま「切替済み」という誤った記述が1日以上正本に残った。
そのあいだ、次のセッションはそれを事実として扱った。

**再発を止める手段**:
- `scripts/verify-oauth-entry.mjs`。ブラウザもGoogleアカウントも要らず、
  本番が実際にどの `client_id` へつないでいるかを1コマンドで出す。
  切替えたときは `--expect-client-id <値>` を渡して走らせる。一致しなければ exit 1 する。
- `AGENTS.md` §7。外部サービスの設定が効いたかどうかを、ユーザーの目視報告だけで
  「確認済み」と書かない。

---

## F-03 引継ぎを未マージのPRに置いたまま終わった

**いつ**: 2026-09-05。

**何が起きたか**: 引継ぎの更新を PR #10 に置いたままセッションを終えた。次のセッションは
`main` の `HANDOFF.md`（8月31日時点）を読み、P9ではなく古い未解決事項を報告した。

**再発を止める手段**: `AGENTS.md` §4「引継ぎは `main` に到達して初めて完了とする」。
`.claude/hooks/session-start.sh` が、`HANDOFF.md` を触っていて `main` に未到達の
ブランチを開始時に名指しする。

---

## F-04 状態欄に前回の文言をそのまま転記した

**いつ**: 2026-09-06。

**何が起きたか**: `IMPLEMENTATION_PLAN.md` のP3〜P6の状態欄が「DB未適用」
「Edge Function未デプロイ」のまま残っていた。実態はDB適用済み、
メール送信コードはそもそも存在しない、だった。前回の文言をコードの再確認なしに
転記したのが原因である。

**再発を止める手段**: `AGENTS.md` §4「状態欄は前回の文言をそのまま転記しない」。
`.claude/hooks/session-start.sh` が、フェーズ表の各行の最終更新日とコードの最終更新日を
比べ、コードの方が新しい行を再確認候補として開始時に列挙する。

---

## F-05 決定済みの仕様をユーザーへ聞き直した

**いつ**: 2026-09-06。

**何が起きたか**: `MEMBERSHIP_FEATURE_SPEC.md` に「参加は会員登録不要」
「AIueoは当事者にならない」と明記されているのに、エージェントがその決定を
ユーザーへ聞き直した。

**再発を止める手段**: `CLAUDE.md` の `@` 参照による強制読み込み、
`AGENTS.md` §4 の必読順序、`.claude/hooks/session-start.sh` の目次表示の三重化。

---

## F-06 例外を握り潰し、原因がログにも残らなかった

**いつ**: 2026-09-05（管理者操作）、2026-09-10（会員登録）。

**何が起きたか**: `catch { rollback; return false }` にログが無く、
危険な企画の非公開化や会員停止が失敗しても管理者は成功と区別できなかった。
同じ形が会員登録の `completeProfileAction` にも残っており、**本番で会員登録が
一度も成功していなかったのに、何が失敗したのかサーバーログにも残らなかった**。
原因（`could not determine data type of parameter $2`）が分かったのは、
ログ出力を足してユーザーがVercelの画面から貼ってくれた後である。

**再発を止める手段**: 例外を握り潰さない。個人情報を含まない形で `console.error` に残す。
新しく `catch` を書くときは、必ず原因が残る形にする。

---

## F-07 「Googleの画面にAIueoと出る」を根拠なく書いた

**いつ**: 2026-09-09。F-02 と同じ件だが、教訓の向きが違うので分けて残す。

**何が起きたか**: 実際には一度も見ていない画面の表示内容を、引継ぎに
「確認（自前の鍵が効いている証拠）」と書いた。書いた時点でその画面は見ていない。

**再発を止める手段**: 自分が見ていないものを「確認した」と書かない。
見ていないなら「未確認」と書く。`AGENTS.md` §7。
