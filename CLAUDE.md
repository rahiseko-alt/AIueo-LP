@AGENTS.md

# 見逃し防止 1/3 — 決定事項の正本を毎回読み込む

このプロジェクトには**決定済みの大計画がある**。行き当たりばったりで設計してはならない。
下の3文書は、作業を始める前に必ず参照すること。ファイル名は次のとおり。

- `MEMBERSHIP_FEATURE_SPEC.md` — **決定仕様の正本**。画面構成の全URL表、会員登録フロー、企画登録フロー、状態モデル、規約の項目、公開前の必須設計。**ここに書いてあることをユーザーへ聞き直さない。**
- `IMPLEMENTATION_PLAN.md` — 常設の進捗台帳。フェーズ、依存関係、受け入れ条件、更新履歴。
- `HANDOFF.md` — 前回セッションからの引継ぎ。現在の状態、確定した問題リスト、ユーザー判断の結果。

以下の `@` 参照で、これらは自動的に読み込まれる。読み込まれていない場合は手で開くこと。

@MEMBERSHIP_FEATURE_SPEC.md
@IMPLEMENTATION_PLAN.md
@HANDOFF.md

## この仕組みを入れた理由

2026-09-06、`MEMBERSHIP_FEATURE_SPEC.md` に「参加は会員登録不要」「AIueoは当事者にならない」と
明記されているにもかかわらず、エージェントがその決定をユーザーへ聞き直した。決定済みの事項を
問い直すのは、計画が無いのと同じ結果になる。

同じ見逃しを3か所で止める。

1. **本ファイル（`CLAUDE.md`）** — `@` 参照で強制的に読み込む
2. **`AGENTS.md` §4** — 開始時の必読順序に `MEMBERSHIP_FEATURE_SPEC.md` を明記
3. **`.claude/hooks/session-start.sh`** — 開始時に3文書の所在・更新日・目次を画面へ出す
