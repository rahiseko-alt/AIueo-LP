# Gate 1 受け入れ条件・実装提案（承認待ち）

作成日: 2026-08-31  
状態: **3視点の敵対検証完了。ユーザー承認前のため実装禁止**

この文書は、`IMPLEMENTATION_PLAN.md`のGate 1を完了させるための提案である。ここに書かれた選択肢・受け入れ条件は、敵対検証後にユーザーへ提示し、承認されたものだけを実装する。

## 推奨する決定

| ID | 決定 | 推奨 | 理由 |
| --- | --- | --- | --- |
| G1-01 | 外部認証 | Google OAuthを基本、メールマジックリンクを代替手段として併設 | Googleを使わない企画者も登録でき、パスワードをAIueoで扱わない |
| G1-02 | 通知メール | `noreply@kouheikosehira.com`を送信者にし、認証済みドメインのSMTP/送信サービスで送る | 認証・期限通知を同じ運用で確実に送る。`info@...`は受信窓口のまま残す |
| G1-03 | 投稿物の権利 | 投稿者の権利を残し、AIueoへ掲載・表示・バックアップ・違反対応に必要な非独占ライセンスを付与 | 「一切帰属」より共同制作者・画像権利者との衝突を避けられる |
| G1-04 | 会員年齢 | 企画者として登録できるのは18歳以上 | 未成年向けイベントは掲載可。ただし参加者情報をAIueoへ入力しない |
| G1-05 | 3日前の扱い | 公開から除外する`auto_hidden`（論理削除） | ユーザーからは削除と同じ表示にしつつ、理由・企画履歴・異議の証跡を残せる |
| G1-06 | 管理者編集 | 全項目を操作可能。ただし元版、差分、理由、企画者への通知を必須 | ユーザー指定の全権限を維持し、恣意的な変更も追跡可能にする |
| G1-07 | 保存・退会 | 具体的な保持表を規約確定時に決める。少なくとも停止・退会後は異議期間中の読取専用アクセスを残す | 論理削除・監査ログ・プライバシーの扱いを矛盾させないため |

## 全フェーズ共通の受け入れ条件

- AIueoは会費・参加費・報酬・経費を受け取らず、参加申込・決済・参加者名簿を保存しない。
- メンバー登録は外部認証と規約同意で即時`active`化する。人による承認待ち・企画審査は作らない。
- 認可は画面非表示だけで済ませず、サーバー側とRLSの両方で強制する。
- 管理者または期限ジョブによる状態変更には、実行者、日時、理由、変更前後、対象規約版を追記専用で記録する。
- 公開情報以外（企画者の認証情報、メッセージ、監査ログ、通報）は、本人と管理者以外に返さない。

## 3視点の敵対検証で追加した必須条件

### 権限・セキュリティ

1. RLSの有効化だけで完了としない。`anon`、一般会員、企画者、停止会員、管理者、Cron/Edge Functionごとに、`select`/`insert`/`update`/`delete`の許可・拒否マトリクスを作り、DB直結テストで検証する。
2. `anon`は公開済み企画の公開列だけを読める。会員は他人のプロフィール、下書き、メッセージ、通報、同意履歴、監査ログを読めない。公開ビューを作る場合もRLSを迂回しない構成にする。
3. `owner_id`、`role`、会員状態、同意、監査列はブラウザから書き換えられない。企画の重要な状態遷移はRoute HandlerまたはDB関数で有限遷移として検証する。
4. 初期管理者の付与はmigration/CLIのみで行い、本人による昇格を不可にする。管理権限の変更時はセッションを更新し、管理者には多要素認証を必須にする。
5. 監査ログは全アプリロールで更新・削除を禁止し、操作時に原子的に追加する。記録するのは実行者、役割、時刻、理由、変更前後、規約版、相関IDであり、不要な本文やメールアドレスを複写しない。
6. `service_role`やメールAPI鍵はブラウザ、`NEXT_PUBLIC_*`、ログ、例外表示に出さない。Cronは専用SecretでEdge Functionを呼び、公開関数を認証なしにしない。

### 会員・企画者体験

1. `active`化は「公開名、必須プロフィール、3文書への明示同意、確認済みメール」がすべて記録済みの場合だけとする。OAuth後に途中離脱した場合は`pending_profile`として登録再開のみ可能にする。これは人による承認待ちではない。
2. 公開操作はサーバーで必須項目、最新規約同意、禁止事項・金銭・掲載権限の確認を同一処理で検証する。失敗時は`draft`のままフィールド別エラーを返す。`submitted`は初期版では内部の短い処理状態とし、企画者や公開利用者に審査待ちと誤認させない。
3. 金銭条件は`none`、`fixed_fee`、`range_or_upper_limit`、`reimbursement`、`reward`、`donation`、`undecided`から必須選択とする。`undecided`は公開不可。`none`以外では金額/上限、通貨、支払先、徴収方法、精算・返金・中止時の扱い、変更条件を常時表示する。
4. 公開後の重要変更（日程、開催形式、金銭、主催者、参加方法）は版履歴と差分を残す。企画者は外部参加者へ通知済み/対象なしを確認して保存する。
5. 企画別メッセージは企画者と管理者だけが送受信・閲覧できる。停止・`auto_hidden`後も閲覧と異議送信は残す。添付ファイルと参加者チャットは初期版に含めない。

### 期限・通知・運用

1. 状態を以下で固定する。物理削除は初期版で行わない。

| 状態 | 公開一覧/詳細 | 企画者の編集 | 異議・メッセージ | 再公開 |
| --- | --- | --- | --- | --- |
| `draft` | 非表示 | 可 | 可 | 可 |
| `published` | 表示 | 可 | 可 | 可 |
| `needs_revision` | 非表示 | 可 | 可 | 企画者が修正後に可 |
| `auto_hidden` | 非表示 | 可 | 可 | 企画者が候補日時を更新後に可 |
| `hidden` | 非表示 | 不可 | 可 | 管理者のみ |
| `ended` | 公開アーカイブ表示（初期版） | 不可 | 可 | 管理者のみ |
| `cancelled` | 詳細に中止表示、一覧から除外 | 不可 | 可 | 管理者のみ |
| `expired` | 非表示 | 可 | 可 | 企画者が再掲載時に可 |

2. 自動処理は開催候補日時を必須にする案を推奨する。候補日時を持たない企画を公開可能にする場合は、7日前/3日前処理の別の起点をユーザーが指定するまで公開不可とする。
3. CronはJSTで期限を判定し、実行直前に`published`かつ`planning`かつ対象候補日時かを再判定する。同じ企画・同じイベントで通知を二重送信しないoutbox一意キー、再試行、失敗記録、実行IDを持つ。
4. 通知メールには機微情報や直接の権限リンクを含めない。ログイン後のマイページ/対象スレッドに遷移させ、メール失敗時もサービス内通知を残す。バウンス・再送・送信事業者Webhookの署名検証を行う。
5. 管理者の編集、非公開、削除、停止は理由コードと自由記述を必須にする。版履歴、差分、監査、サービス内通知は同一操作で作成する。メール失敗は措置を戻さず、通知失敗として管理画面から再送できるようにする。

## 受け入れ前にユーザーが選ぶ項目

以下は、上の推奨で進めるか、別案を指示して確定する。

1. G1-01: Google OAuth + メールマジックリンクでよいか。
2. G1-02: 通知用の送信者を`noreply@kouheikosehira.com`として、ドメイン認証済みSMTP/送信サービスを使ってよいか。実アカウント作成・DNS設定は必要な時点であなたの権限が必要。
3. G1-03: 投稿物は「AIueoへ必要範囲の非独占ライセンス」を採用してよいか。
4. G1-04: 企画掲載会員を18歳以上に限定してよいか。
5. G1-05: 「自動削除」は物理削除ではなく、公開から除外する`auto_hidden`でよいか。
6. G1-06: 管理者の全項目編集を維持し、理由・差分・通知を必須にしてよいか。
7. G1-07: 開催候補日時を公開時に必須としてよいか。候補日時なしで公開したい場合は、7日前/3日前判定の起点を指定してほしい。
8. G1-08: データ保持期間・退会後の読取専用期間は、規約の最終確認と同時に確定する。暫定値をこちらで提案してよいか。

## 既存インフラの確認結果

- Vercel Productionには、177日前に設定された`VITE_SUPABASE_URL`と`VITE_SUPABASE_ANON_KEY`がある。一方、Next.js用の`NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`、サーバー専用Secretは未設定であり、Preview/DevelopmentにもSupabase変数はない。
- 既存のSupabaseプロジェクト・データがある可能性がある。接続先と既存テーブルを確認する前に、上書き・削除・migration適用を行わない。
- `GMAIL_USER`、`GMAIL_APP_PASSWORD`、`STRIPE_SECRET_KEY`も既存設定として検出した。AIueoは金銭を扱わない方針のためStripe鍵は本機能では使用しない。メールは既存Gmailを使うか、専用送信者・送信基盤を新設するかをG1-02で確定する。
- P1開始時には、Supabaseプロジェクトへの管理アクセス、Google OAuthのClient ID/Secret、送信ドメインまたはSMTP/メール送信サービスの設定権限が必要になる。秘密値そのものは会話やGitへ出さず、Vercel/SupabaseのSecretとして設定する。

## P1 基盤の受け入れ条件

1. `@supabase/ssr`でcookieベースのSSR認証を実装し、`auth-helpers`は導入しない。
2. DBマイグレーションで、`profiles`、`terms_versions`、`consents`、`proposals`、`proposal_versions`、`proposal_messages`、`reports`、`notifications`、`moderation_actions`、`audit_log`を作成する。
3. 各公開スキーマのテーブル/ビューでRLSを有効化し、`anon`、`authenticated`、管理操作の許可・拒否を操作別にテストする。
4. `role`、会員状態、監査ログ、管理者操作はクライアントから直接更新できない。
5. 開発・本番の環境変数、Supabaseプロジェクト、メール秘密情報を分離し、秘密鍵はVercel/SupabaseのSecretにのみ保存する。

## P2–P3 公開文書・会員登録の受け入れ条件

1. `/terms`、`/disclaimer`、`/privacy`、`/register`を公開する。登録画面では送信前に各文書へ到達でき、利用目的を読める。
2. 規約への同意は、規約版、本文ハッシュ、同意日時を保存する。重要改定時は再同意を求める。
3. Google OAuthまたはマジックリンクで認証し、確認済みメールがある会員だけを`active`化する。
4. 登録必須情報は公開名、企画・協力したい内容、18歳以上の確認、規約同意。氏名・住所・電話番号・参加者情報は取得しない。
5. 停止済み会員は新規公開・編集をできないが、自分の企画・措置理由・メッセージを読み取り専用で確認できる。

## P4 企画機能の受け入れ条件

1. 有効会員だけが`/member/proposals/new`を利用できる。未ログイン・停止済み会員はサーバー側で拒否する。
2. 入力必須は、企画名、概要、開催形式、主催者、参加方法、公開範囲、公開期限。金銭がある場合は参加費・報酬・経費・精算方法・返金/中止時の扱い・支払先を必須にする。金銭がなければ「なし」を明記する。
3. 企画者は`draft`、`submitted`、`published`、`ended`、`cancelled`を自分で遷移できる。`submitted`は事前審査待ちではなく、入力検証後に公開するための処理状態とする。
4. 管理者は`needs_revision`、`hidden`、削除を含む全状態・全項目を操作できる。操作時には理由と変更差分が必須。
5. 公開時に、禁止事項、金銭条件、掲載権限の宣言を必須にする。公開後は誰でも通報できる。

### 開催日と自動期限の未解決点

「開催候補日または募集期限」だけでは、開催日の7日前・3日前を正しく計算できない。推奨は、公開企画に**開催候補日時（JST）を必須**とし、募集期限は別任意フィールドにすること。

この推奨を採用する場合の受け入れ条件:

- 開催候補日時の7日前、JSTの定時ジョブで`confirmed`でない企画者へ注意メールとサービス内通知を1回送る。
- 開催候補日時の3日前、`confirmed`でなければ`auto_hidden`として公開一覧・詳細から除外する。実行直前に再判定し、実行後に通知・監査ログを残す。
- 企画者は開催候補日時を更新して再掲載できる。既に`confirmed`なら自動非公開しない。

## P5–P6 管理・メッセージ・通知の受け入れ条件

1. 企画者と管理者だけが読める、企画ごとのテキストメッセージを提供する。添付ファイル・参加者チャットは初期版の範囲外。
2. 管理者の非公開・削除・編集・会員停止では、企画者にサービス内通知とメールを送る。理由・根拠の規約版・処理日時を表示する。
3. メールは認証済みの送信ドメインから送り、送信結果（成功・失敗）を保存する。送信失敗時はサービス内通知を残す。
4. Supabase CronからEdge Functionを呼び、期限通知・自動非公開の重複実行を防ぐ。ジョブ失敗は管理者ダッシュボードに出す。

## P7 本番受入の受け入れ条件

1. 3視点（権限、企画者/参加者、運用・法務）の敵対検証で出た必須指摘を解決またはユーザーが明示的に受容する。
2. RLSの許可/拒否テスト、主要フローのE2E、メール送信のステージング確認、期限ジョブの時刻操作テストを通す。
3. 360px、768px、1280pxで登録・企画・管理画面を確認し、タップ領域と横はみ出しがない。
4. Vercel本番で、未ログイン・会員・管理者の権限分離と公開/非公開の表示を確認する。
