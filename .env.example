# Browser-safe values: configure independently for Development, Preview, and Production.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only values. Never prefix these with NEXT_PUBLIC_ and never commit their values.
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=
